class Asteroid extends PVector {
  PVector  pPos;
  PVector  vel; 
  PVector  acc;
  float    mass;

  void update(Planet[] p) {
    /*
     * loops through p
     * gravity physics
     * moves particles
     * checks edges
     * checks collisions w/ planets root(p.mass/pi)
     */
  }

  void draw() {
    /*
     * get color scheme
     * line(x,y,pPos.x,pPos.y);
     * pPos.set(x,y,0);
     */
  }
}

